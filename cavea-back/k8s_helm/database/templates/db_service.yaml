apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-service
  namespace: {{ .Release.Namespace }}
spec:
  selector:
    app: {{ include "database.name" . }}
  ports:
    - protocol: TCP
      port: 3306
      targetPort: 3306