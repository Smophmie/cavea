name: Backend CI

on:
  push:
    branches: [ main, dev, feature/ci-cd ]
  pull_request:
    branches: [ main ]

jobs:
  tests:
    uses: ./.github/workflows/jobs/common/laravel-tests.yaml@feature/ci-cd

  sonarqube:
    needs: tests
    uses: ./.github/workflows/jobs/common/laravel-sonarqube.yaml@feature/ci-cd

  package:
    needs: sonarqube
    uses: ./.github/workflows/jobs/common/laravel-package.yaml@feature/ci-cd

  deploy:
    needs: package
    uses: ./.github/workflows/jobs/common/laravel-and-db-deploy.yaml@feature/ci-cd